{
  "name": "detroit-data-intel-v2",
  "version": "1.0.0",
  "description": "Detroit Data Intelligence Platform V2",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "test": "npx playwright test",
    "test:api": "node --test tests/*.node.test.js",
    "test:refresh": "python3 -m unittest discover -s tests -p 'test_*.py'",
    "test:ui": "npx playwright test --ui"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.1",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.1"
  }
}
